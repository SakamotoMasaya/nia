<%= stylesheet_link_tag    'users', media: 'all', 'data-turbolinks-track': 'reload' %>
<% if logged_in? %>
    <%= render 'relationships/follow_button', user: @user %>
<% end %>
<div class="user-show">
    <div class="container">
        <div class="user">
            <%= image_tag "default/human.png" %>
            <h2><%= @user.name %></h2>
        </div>
    </div>
</div>